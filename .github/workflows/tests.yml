name: CI

on:
  pull_request:
    branches:
      - main
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: python

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Analyze
        uses: github/codeql-action/analyze@v3
        with:
          category: /language:python

  lint-format:
    name: Lint and format
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen --group dev

      - name: Run Ruff
        run: make lint

      - name: Check Black formatting
        run: uv run black --check src tests

  typecheck:
    name: Type check
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen --group dev

      - name: Run mypy
        run: make typecheck

  test:
    name: Tests / Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      # .python-version pins 3.13 and uv honours it over whatever setup-python
      # installed, so without this every matrix leg ran 3.13.
      UV_PYTHON: ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        python-version:
          # requires-python is >=3.12
          - "3.12"
          - "3.13"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen --group dev --extra langfuse

      - name: Run tests
        run: make test

  security:
    name: Dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen --group dev

      - name: Audit Python dependencies
        run: |
          uv export --frozen --group dev --format requirements-txt --no-hashes --output-file requirements-audit.txt
          # Drop local/editable refs (the lemoncrow package itself, vendored stub wheels) —
          # they aren't published to PyPI so pip-audit can't look them up and errors out.
          grep -v -E '^(-e |\./|\.\./)' requirements-audit.txt > requirements-audit-filtered.txt
          # lc-debt: fixes blocked by a pin, not accepted risk. mitmproxy (benchmarks
          # only; no newer release) pins cryptography<=48.1 and h2==4.3.0, so the fixed
          # versions cannot resolve. Drop these ignores once mitmproxy lifts the pins.
          uvx pip-audit --requirement requirements-audit-filtered.txt \
            --ignore-vuln PYSEC-2026-3552 \
            --ignore-vuln PYSEC-2026-3553 \
            --ignore-vuln PYSEC-2026-3554 \
            --ignore-vuln PYSEC-2026-3628

  macos-install:
    name: macOS install script (bash 3.2 compat)
    runs-on: macos-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify bash version
        run: bash --version | head -1

      - name: Run dev installer syntax check with macOS bash
        run: bash -n scripts/local.sh

      - name: Run install.sh syntax check
        run: bash -n scripts/install.sh

      - name: Run dev installer dry-run
        run: |
          bash scripts/local.sh --dry-run --no-hosts --no-stack --non-interactive --verbose
